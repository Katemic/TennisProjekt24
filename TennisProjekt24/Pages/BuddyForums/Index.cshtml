@page
@model TennisProjekt24.Pages.BuddyForums.IndexModel
@{
    ViewData["Title"] = "Index";
}
<h4>Makker forum</h4>

<a asp-page="/BuddyForums/CreateBuddyForum">Opret</a>

<table class="table">
    <thead>
        <tr>
            <th>
                PostId
            </th>
            <th>
                Dato
            </th>
            <th>
                MedlemsId
            </th>
            <th>
                Title
            </th>
            <th>
                Text
            </th>
            <th>
                Erfarings niveau
            </th>
        </tr>
    </thead>
    <tbody>
        @if (Model.buddyForums.Count() != 0)
        {
            @foreach (var item in Model.buddyForums)
            {
                <tr>
                    <td>
                        @item.PostId
                    </td>
                    <td>
                        @item.DateTime
                    </td>
                    <td>
                        @item.MemberId
                    </td>
                    <td>
                        @item.Title
                    </td>
                    <td>
                        @item.Text
                    </td>
                    <td>
                        @item.SkillType
                    </td>
                    <td>
                        <p>
                            <a asp-page="/Courts/DeleteCourt" asp-route-courtId="@item.PostId">Slet</a>|
                            <a asp-page="/Courts/UpdateCourt" asp-route-courtId="@item.PostId">Opdater</a>|
                        </p>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>